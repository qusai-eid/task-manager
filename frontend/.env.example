# Optional — only needed if you switch backends.
# The Railway URL is already baked into api.ts as the production default.
# Set this in Vercel's Environment Variables to override it.
VITE_API_URL=https://task-manager-production-097d.up.railway.app/api
