# Copy to .env.local for local development (Vite proxy handles /api automatically, so this is optional locally)
# In Vercel: add VITE_API_URL as an Environment Variable pointing to your Railway backend

# Example (replace with your actual Railway URL after deploying the backend):
VITE_API_URL=https://YOUR-PROJECT.up.railway.app/api
